# In production, use External Secrets Operator or Sealed Secrets — never commit real credentials
apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: nexusgrid
type: Opaque
stringData:
  postgres-password: "postgres"
  jwt-secret-key: "NexusGrid-Super-Secret-Key-For-Development-Only-Min-32-Chars!"
